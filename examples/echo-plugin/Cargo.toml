[package]
name = "echo-plugin"
version = "0.1.0"
edition = "2021"
description = "An echo plugin that demonstrates request handling in the Skylet plugin API"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
skylet-abi = { git = "https://github.com/vincents-ai/skylet.git", branch = "main" }
